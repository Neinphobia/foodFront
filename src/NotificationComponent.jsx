// NotificationComponent.jsx
import React from 'react';

const NotificationComponent = ({ message }) => (
  <div>
    <p>{message}</p>
    {/* Add styling and close button if needed */}
  </div>
);

export default NotificationComponent;
